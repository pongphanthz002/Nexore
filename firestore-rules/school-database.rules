rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all access temporarily for debugging
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
